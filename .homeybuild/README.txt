Copy these files into your app root so BOTH locations exist:
  .homeycompose/drivers/btlight/driver.compose.json
  drivers/btlight/driver.compose.json

Then validate:
  homey app validate --level publish
